# Tasks

1. Add requirements + design artifacts for Bluetooth enablement.
2. Create a NixOS module enabling BlueZ + boot power-on + sane defaults.
3. Import the new module from the NixOS module aggregator.
4. Validate via `nix eval` or `nix flake check` that the configuration evaluates.
